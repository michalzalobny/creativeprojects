import styled from 'styled-components';

import { RichText } from 'components/RichText/RichText';

interface InfoTextProps {}

export const InfoText = styled(RichText)<InfoTextProps>`
  padding: 2rem;
  color: white;
`;
