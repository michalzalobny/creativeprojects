import styled from 'styled-components';
import { motion } from 'framer-motion';

import { DefaultButton } from 'components/Buttons/DefaultButton/DefaultButton';

interface Props {}

export const FooterLink = styled(DefaultButton)<Props>``;
