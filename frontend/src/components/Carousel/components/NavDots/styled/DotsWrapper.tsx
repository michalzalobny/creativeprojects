import styled from 'styled-components';
import { motion } from 'framer-motion';

interface Props {}

export const DotsWrapper = styled(motion.div)<Props>`
  display: flex;
  justify-content: center;
`;
