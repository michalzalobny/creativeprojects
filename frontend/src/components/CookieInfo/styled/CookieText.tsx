import { motion } from 'framer-motion';
import styled from 'styled-components';
import { sharedValues } from 'utils/sharedValues';

export const CookieText = styled(motion.p)`
  margin-bottom: 1rem;
`;
