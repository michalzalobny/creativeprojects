import styled from 'styled-components';
import { motion } from 'framer-motion';

import { MyCarousel } from '../components/MyCarousel/MyCarousel';

interface Props {}

export const FarmerCarousel = styled(MyCarousel)<Props>``;
