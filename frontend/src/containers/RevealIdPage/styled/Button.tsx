import styled from 'styled-components';
import { motion } from 'framer-motion';

import { BlobButton } from 'components/Buttons/BlobButton/BlobButton';

interface Props {}

export const Button = styled(BlobButton)<Props>``;
