import styled from 'styled-components';
import { motion } from 'framer-motion';

import { RichParagraph } from 'utils/styled/shared/RichParagraph';

interface Props {}

export const InfoParagraph = styled(RichParagraph)<Props>``;
